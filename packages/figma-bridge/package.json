{
  "name": "@ds-cli/figma-bridge",
  "version": "1.0.0",
  "description": "Figma integration - plugin generation and API push",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:git-ops": "vitest git-operations.test.ts",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@ds-cli/core": "workspace:*",
    "chalk": "^4.1.2",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^1.1.0",
    "typescript": "^5.3.0",
    "vitest": "^1.1.0"
  }
}
